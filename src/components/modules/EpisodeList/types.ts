export type TWork = {
  id: string
  state: string
  image: string
  title: string
  media: string
}

export type TEpisode = {
  id: string
  numberText: string
  title: string
  viewerDidTrack: boolean
}
